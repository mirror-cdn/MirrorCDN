@page "/make"
@inject NavigationManager navigationManager
<h1>链接伪装(头部请保留http/https)</h1>
<center>
<br />
源链接：
<input @bind-value=@InputUrl style="width:50%;margin:5px" />
<br />
加密后：
<input @bind-value=@OutputUrl style="width:50%;margin:5px" />
<br />
<Button OnClick="@CreateUrl" Text="生成" Color="Color.Primary" />
</center>
@code{
    [Parameter]
    public string InputUrl { get; set; } = "";
    [Parameter]
    public string OutputUrl { get; set; } = ""; 
    public void CreateUrl()
    {
        if (string.IsNullOrEmpty(InputUrl)==false)
        {
          OutputUrl = "https://mirror-cdn.github.io/api?Url="+ HttpUtility.UrlEncode(Ext.StringToGZip(InputUrl));  
        }        
    }
} 